name: Checks ➜ Tests ➜ Publish

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  main:
    uses: nodutilus/project-actions/main.yml@test
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
